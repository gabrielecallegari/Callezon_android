<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginHorizontal="12dp"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    app:cardElevation="4dp">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <androidx.cardview.widget.CardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="12dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="0dp"
            android:id="@+id/wrapper"
            >
            <ImageView
                android:id="@+id/immagineCarta"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_launcher_background"
                android:contentDescription="OGGETTO"
                />

        </androidx.cardview.widget.CardView>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif"
            android:textColor="#222"
            android:textSize="18sp"
            android:textStyle="bold"
            android:id="@+id/nomeCarta"
            tools:text="NomeProdotto"
            android:layout_alignTop="@id/wrapper"
            android:layout_toEndOf="@id/wrapper"/>

        <TextView
            android:fontFamily="sans-serif"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/nomeCarta"
            android:layout_toEndOf="@id/wrapper"
            tools:text="39€"
            android:textSize="15sp"
            android:id="@+id/prezzoCarta"
            />
        <TextView
            android:fontFamily="sans-serif"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/prezzoCarta"
            android:layout_toEndOf="@id/wrapper"
            tools:text="Qta. disponibili: 33"
            android:textSize="15sp"
            android:id="@+id/quantitaCarta"
            />

    </RelativeLayout>

</androidx.cardview.widget.CardView>